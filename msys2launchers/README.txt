BUILD WITH ucrt64 ENVIRONMENT (not msys2 itself)

Used to create exe wrappers to run msys2 shells with proper environment setup.
These are compiled to an exe so that they can run from windows without being cmd/bat scripts (with their annoying "terminate batch script" prompt)
